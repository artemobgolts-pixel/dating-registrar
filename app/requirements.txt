# Версии зафиксированы намеренно: пересборка контейнера через год
# не должна молча менять поведение зависимостей.
fastapi==0.136.3
uvicorn[standard]==0.49.0
jinja2==3.1.6
python-multipart==0.0.32
itsdangerous==2.2.0
httpx==0.28.1
pillow==12.3.0
tzdata==2026.2
pillow-heif==1.4.0    # HEIC/HEIF с айфонов «как есть»
segno==1.6.6          # QR-код ссылки на сервере (чистый Python, без PIL)
